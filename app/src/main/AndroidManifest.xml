<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vocabbuilder">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VocabBuilder">
        <activity android:name=".LoginPhoneActivity"></activity>
        <activity android:name=".ChosenWordActivity" />
        <activity android:name=".ChosenDateActivity" />
        <activity android:name=".PractisedWords" />
        <activity android:name=".UpdatePhoto" />
        <activity
            android:name=".ImageActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity
            android:name=".UpdateProfile"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".WordsActivity" />
        <activity android:name=".SigninActivity" />
        <activity
            android:name=".MainActivity"
            android:configChanges="screenLayout|orientation|screenSize" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.VocabBuilder.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>